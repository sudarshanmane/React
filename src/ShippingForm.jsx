import React from "react";

const ShippingForm = ({ handleSubmit }) => {
  return (
    <div>
      <button onClick={() => handleSubmit}>ShippingForm</button>
    </div>
  );
};

export default ShippingForm;
